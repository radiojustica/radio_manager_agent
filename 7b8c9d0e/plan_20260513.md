# Implementation Plan: Advanced Music Analytics
**Ticket ID:** 7b8c9d0e
**Persona:** Pickle Rick (O Cientista, não o Jerry)

## Phase 1: Database & Model Update
1. **`core/models.py`**:
    - Adicionar `bpm = Column(Integer, default=0)`
    - Adicionar `valence = Column(Float, default=0.5)`
    - Adicionar `danceability = Column(Float, default=0.5)`
2. **`core/database.py`**:
    - No `init_db`, adicionar blocos `try/except` para os novos campos usando `ALTER TABLE`.

## Phase 2: Advanced Worker Logic
1. **`services/curadoria_worker.py`**:
    - Renomear/Refatorar `calcular_energia_librosa` para `analisar_acustica_completa`.
    - Implementar `librosa.beat.beat_track` para BPM.
    - Implementar heurística de Valência (Brilho Espectral + Modo).
    - Implementar heurística de Dançabilidade (Onset Strength Mean).
    - Atualizar `processar_arquivo` para capturar esses novos valores.

## Phase 3: Integration
1. **`services/curadoria_worker.py`**:
    - Atualizar `_job_processar_lote` para salvar os novos campos no banco de dados após a análise.

## Phase 4: Verification
1. Criar `tests/test_audio_analysis.py`.
2. O teste deve carregar um arquivo de áudio conhecido e verificar se o BPM e as métricas estão dentro de intervalos razoáveis (ex: BPM entre 40 e 220).
3. Executar o worker manualmente para um lote pequeno e verificar o DB.

**Nota:** Não use `npm install` para bibliotecas Python, Morty. Isso é estúpido. Use o que já temos.
